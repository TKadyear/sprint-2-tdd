{
  "name": "sprint-2-tdd",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^28.1.0"
  }
}
